# 地铁路网沙盒系统场景式操作手册（小白友好版）

本手册专为地铁爱好者与图论探索新手设计。抛弃繁琐的学术名词，我们从您最可能遇到的实际场景出发，解答如何通过简单的点击完成各种极限地铁路线的探索。

## 1. 快速场景与操作指南

### 场景一：想找出全城最长的一条地铁环线（不重复走相同路段）
- **您的目标**：想买一张票，在地铁里坐最久，最后能坐回起点，且中途不走回头路。
- **操作方法**：
  1. 在页面顶部选择您的城市（例如：`广佛`）。
  2. 在下方底栏导航中，点击进入 `规划` 面板。
  3. 将 `规划模式` 切换为 `闭环回路`（表示首尾相接）。
  4. 点击地图上的任意车站（例如：`西塱`），在弹出的菜单中点击 `设为起点`。
  5. 点击底部的 `开始计算` 按钮。系统将自动算出并高亮展示以此为起点的最长不重复环线，并在右侧展示乘车路书。

### 场景二：两站之间规划路线，但想中途去某些站“打卡”，或者避开某些“拥堵站”
- **您的目标**：规划一条从 A 站到 B 站的最长单向路线，但中途必须经过 C 站（打卡），且绝不经过 D 站（避让）。
- **操作方法**：
  1. 在 `规划` 面板中，将模式设为 `单向路径`，并设置好 `起点站`（A站）与 `终点站`（B站）。
  2. 进入底栏导航的 `约束` 面板。
  3. 在地图上找到 C 站点击它，在弹出菜单中选择 `设为必经`（此时 C 站会加入打卡清单）。
  4. 在地图上找到 D 站点击它，选择 `设为避让`（此时 D 站会变成红色，代表不可通行）。
  5. 点击 `开始计算`，系统将绕过 D 站，并在串联 C 站的前提下，为您算出 A 到 B 的最长地铁路线。

### 场景三：计算时间太久，界面一直显示“正在计算”，感觉电脑快卡死了
- **您的目标**：大城市（如北京、上海）车站极多，计算最长路线时状态探索数破百万，想立刻停下来。
- **操作方法**：
  1. 页面在计算时会弹出一个半透明的加载遮罩，显示“正在计算...”。
  2. 此时，请直接点击遮罩层底部的 `取消计算` 按钮。
  3. 算法会立即安全中止，并把当前已经搜索到的最优路径回填到地图上展示，不会导致浏览器无响应。

### 场景四：觉得右侧的文字路书太枯燥，想直观模拟坐车全过程
- **您的目标**：想以第一人称或列车视角，看着车在线路上动起来。
- **操作方法**：
  1. 规划好路线并高亮后，点击右侧路书抽屉下方的 `模拟巡游` 按钮（或点击右上角出现的巡游悬浮框播放按钮）。
  2. 地图上会出现发光列车在轨道上匀速移动，且整条高亮路线会像流体一样闪烁。
  3. 勾选巡游面板中的 `视角跟随`，地图会自动跟着车头移动，不用自己用鼠标拖拽。
  4. 觉得开得太慢，可以点击 `2x`、`4x` 或 `8x` 按钮加速。如果想一直看，可以勾选 `循环播放`。

### 场景五：想找出整个城市最远的“通勤挑战”组合
- **您的目标**：想知道在当前城市里，哪两站之间坐地铁“折腾的站数最多”，并一步到位在地图上高亮。
- **操作方法**：
  1. 点击底部导航的 `分析` 按钮，选择打开 `直达矩阵`。
  2. 弹窗中会呈现一张排行榜，列出了“起始站”、“终点站”和“经过最少车站数”。表格默认是按车站数从大到小（降序）排列的。
  3. 比如第一行显示 `A站 - B站 : 60站`，代表这两人通勤最漫长。
  4. 用鼠标直接点击 `60` 这个数字，直达矩阵弹窗会自动关闭，地图会立刻定位并高亮这条路线，并在右侧为您生成换乘路书。

### 场景六：想找出哪些站是“超级咽喉枢纽”（一旦出事全网瘫痪）
- **您的目标**：作为地铁路网研究者，想找出隐藏的交通命脉车站。
- **操作方法**：
  1. 点击底部的 `分析` 按钮，选择打开 `度/介数` 排行榜。
  2. 在图空间里选择 `Space L`（轨道空间）。
  3. 查看 `介数` 那一列（数值范围为 $0$ 到 $1$ 的 5 位小数）。数值越接近 $1$，说明全网有越多的乘车路线必须经过该站。
  4. 在列表中点击该站名，排行榜会自动收起，地图会平移聚焦到该站，并带有紫色光圈闪烁高亮，方便您在空间上定位。

### 场景七：大城市的路线太复杂，不想等计算，想直接看已经算好的“全城最长路线”
- **您的目标**：直接查看官方终极最优大环线或最长单向线，秒级渲染。
- **操作方法**：
  1. 点击底部的 `分析` 按钮，选择打开 `城市线网之最`。
  2. 在下拉框中选择要查看的城市，然后在下方单选框里勾选规则（例如：`不重复站点最长闭环路线`）。
  3. 点击 `查看预计算结果`，下方卡片会立刻呈现终极答案的起点、经过区间数以及换乘数。
  4. 点击卡片上的 `在地图查看并打开路书`，系统将越过计算，瞬间将该极限路线高亮绘制在地图上，并生成分段路书。

## 2. 核心专有名词通俗解释

为了帮助您更好地阅读分析数据，这里将复杂的图论指标翻译成“乘客乘车体感”：
- **Space L（物理轨道空间）**：就是普通的地铁线路图。在这个空间下：
  - **度数**：代表这个站直接连接的邻居站有多少个（比如三线换乘站的度数通常较大）。
  - **介数**：代表这个站的“咽喉程度”。如果它停运，全网会有极高比例的出行被迫绕道。
- **Space P（乘客零换乘空间）**：如果两个站在同一条线（哪怕隔了20站），也算直接相连。在这个空间下：
  - **度数**：代表从这个站出发，**不换乘**能直达的车站总数。度数越大，说明直达性越好。
  - **介数**：代表这个站在“跨线换乘”中的枢纽地位。介数越高，说明大家坐地铁换乘时越依赖在这个站进行线路切换。
- **N站覆盖数**：代表从该站出发，坐 $N$ 站地铁（默认 10 站）以内，一共能去到多少个不同的车站。数值越高，说明该站的交通辐射能力越强。
